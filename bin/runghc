#!/usr/bin/env sh
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

make --silent -f $DIR/env.mk $DIR/../.ghc-env || exit 1;
source $DIR/../.ghc-env

runghc $(for a in $(cabal-cargs -c $DIR/../late-firefly.cabal); do echo "--ghc-arg=$a"; done) -j -O0 "$@"
